"""Apohara 2.0 benchmark suite (in-package).

US-002 skeleton. See `apohara_context_forge/benchmarks/__init__.py` for the
mapping between files and phases. The four stub scripts in this directory
share one philosophy: argparse + `--help` works in Phase 0, real metrics
land later, and the CLI surface is stable so downstream consumers can
write wiring code before numerics are ready.
"""
